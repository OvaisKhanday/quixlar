import { FC } from "react";

interface AppLogoProps {}

const AppLogo: FC<AppLogoProps> = ({}) => {
  return <h1 className='text-green-500 text-3xl font-bold'>Quixlar</h1>;
};

export default AppLogo;
